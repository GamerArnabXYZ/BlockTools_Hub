import 'package:flutter/material.dart';

/* Ye screen Minecraft seeds manage karne ke liye hai (Placeholder) */
class SeedPage extends StatelessWidget {
  const SeedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SEED TOOLS')),
      body: const Center(
        child: Text('Seed Tools coming soon...'),
      ),
    );
  }
}
